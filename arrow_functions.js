// Normal function
const add = function (x, y) {
    return x + y;
}

// Arrow function
const addition = (x, y) => {
    return x + y;
}

// Best simplification
const additionals = (x, y) => x + y;

console.log(add(2,3))
console.log(addition(4,6))
console.log(additionals(5,7))