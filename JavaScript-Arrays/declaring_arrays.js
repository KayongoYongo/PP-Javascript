// Arrays are simple declared using the square bracket notaation
let array = [];

// Arrays in jaascript are Zero-indexed
let nums = [4, 6, 8];
// To access the first element of the array, we do the following
console.log(nums[0])